---
description: Antigravity Agent Workflow Rules: Productivity Dojo
---

1. Standard Operating Procedure (The Workflow)يجب على الـ Agent اتباع هذا التسلسل عند بناء أي ميزة جديدة:Analysis: مراجعة الـ PRD ومعادلة الـ XP: $XP = (P \times 10) + (T_{allocated} / 10)$.Implementation: كتابة الكود باستخدام Vite + React 19 مع الحفاظ على جمالية الـ Neon (Deep Obsidian #0A0A0C).Test Generation: إنشاء ملف اختبار Playwright تلقائياً لكل ميزة (مثلاً features/tasks.spec.ts).Verification: تشغيل Playwright والتأكد من أن الـ UI يعكس التغييرات اللحظية (Optimistic UI) وأن الـ Database Trigger في Supabase قام بتحديث الـ Rank بشكل صحيح.